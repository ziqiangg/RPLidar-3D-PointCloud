# Raspberry Pi Requirements
# Scanner service plus optional CPU RandLA-Net inference runtime.

# RPLidar communication
rplidar-roboticia

# Serial port detection
pyserial

# MQTT communication
paho-mqtt

# Configuration parsing
pyyaml

# Numerical operations
# Pin below NumPy 2 because the tested torch stack still expects the 1.x ABI.
numpy<2
scipy

# Webcam capture for panorama mode
opencv-python-headless

# Servo control (Raspberry Pi)
gpiozero
lgpio

# Optional post-scan segmentation runtime (used when config.segmentation.enabled=true)
# Keep this separate from notebook-only packages: no jupyterlab/ipykernel/plotly here.
--extra-index-url https://download.pytorch.org/whl/cpu/
torch>=2.2,<2.3
